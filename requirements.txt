fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# 数据库
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
aiomysql>=0.2.0 

# LLM
httpx>=0.27.0

# 向量知识库
chromadb>=0.4.22

# 语义 embedding（可选：BGE 检索质量更高，未安装时自动降级为哈希 embedding）
# CPU 可跑，首次运行自动下载 bge-small-zh-v1.5 模型（约 100MB）
sentence-transformers>=2.7.0

# Excel 导出
openpyxl>=3.1.0

# Redis（可选）
# redis>=5.0.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
python-multipart
